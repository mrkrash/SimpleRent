{% extends 'admin.html.twig' %}

{% block title %}Modifica Accessorio{% endblock %}

{% block body %}
    <section class="section">
        <div class="container">
            <div class="level">
                <div class="level-left">
                    <div class="level-item">
                        <h1 class="title">Modifica Accessorio</h1>
                    </div>
                </div>
                <div class="level-right">
                    <div class="level-item">
                        {{ include('accessory/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>

        {{ include('accessory/_form.html.twig') }}

    </section>
{% endblock %}
