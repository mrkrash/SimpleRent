<h1>Una nuova prenotazione</h1>

<p>
  Ciao, {{ customer.firstname }} {{ customer.lastname }} ha effettuato un prenotazione dal {{ booking.dateStart|date('d/m/Y') }} al {{ booking.dateEnd|date('d/m/Y') }}.
</p>
<p>Qui un breve riepilogo:</p>
<table>
  {% for product in booking.bookedProduct %}
    <tr>
      <td>{{ product.product.name }}</td>
      <td>{{ product.size }}</td>
      <td>{{ product.qty }}</td>
    </tr>
  {% endfor %}
</table>